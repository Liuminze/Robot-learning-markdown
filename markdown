1. AI / ML / DL 的关系
2. 人工智能 include 机器学习 include 深度学习。

人工智能：让机器表现出需要人类智能才能完成的能力（很广，包括专家系统、搜索、符号推理等）。
机器学习：AI 的一个分支，让机器从数据中学习规律，而不是靠人手工写死规则。
深度学习：ML 的一个分支，用多层神经网络自动学习数据的层次化特征表示。
2. 监督学习 vs 无监督学习
监督学习：数据有"标准答案"（标签）。模型学的是 输入 → 标签 的映射。
比如：房价预测（输入面积、位置，输出价格）；图像分类（输入图片，输出"猫/狗"）。
无监督学习：数据没有标签，模型自己找结构。
比如：聚类（把客户分成几类）、降维（PCA）、异常检测。
3. 数据、特征、标签；三种数据集
数据：原始样本，比如一张图、一段文本、一条交易记录。
特征：从数据里抽出来、模型能看懂的输入量。比如鸢尾花的花瓣长度。
标签：样本的真实结果，也就是监督学习里的"答案"。
训练集：用来真正训练模型、更新参数。
验证集：用来调超参数、选模型，不能用来训练。
测试集：训练完全结束后，最后一次评估模型泛化能力，训练过程中绝对不能看。
4. Batch size
含义：每次更新模型参数时，用的样本个数。
影响：
batch 小（如 1、16）：噪声大、收敛震荡，但内存占用小、每次更新快。
batch 大（如 256、1024）：梯度估计更稳，但内存压力大，容易陷入尖锐极小值。
极端：batch=1 是随机梯度下降（SGD），batch=全部数据是批量梯度下降（BGD）。
5. 模型与模型参数
模型：一个带有可学习参数的函数/映射。比如神经网络就是一种模型。
模型参数：模型从数据中学到的内部变量。例如线性回归里的权重 w 和偏置 b；神经网络里的权重矩阵和偏置向量。
与之相对的是超参数：人提前设定的，比如学习率、层数、batch size。
6. 神经网络基本组成 + 激活函数
基本组成：输入层 → 隐藏层 → 输出层；层与层之间由权重和偏置连接；每层先算线性变换，再经过激活函数。
激活函数：给网络引入非线性。
为什么要激活函数：如果没有激活函数，多层网络叠在一起仍然只是一个线性变换，表达能力跟单层没区别，无法拟合复杂函数。
常见激活函数：
Sigmoid：输出 (0,1)， historically 常用，现在深层网络基本不用（梯度消失）。
Tanh：输出 (-1,1)，比 Sigmoid 零均值一点。
ReLU：f(x)=max(0,x)，计算快、缓解梯度消失，目前最常用。
7. 矩阵求导
矩阵求导本质是对多元函数求梯度，只是结果按矩阵/向量形式组织。

标量对向量求导 → 梯度向量。
常见公式：∂(Ax)/∂x = Aᵀ，∂(xᵀAx)/∂x = (A+Aᵀ)x。
深度学习中真正用到的是链式法则在向量/矩阵上的推广。
8. 全连接层的前向传播用矩阵表示
一层网络：
z
=
W
x
+
b
z=Wx+b
a
=
f
(
z
)
a=f(z)
其中：

x：输入向量（n×1）
W：权重矩阵（m×n）
b：偏置向量（m×1）
f：激活函数
a：输出/激活值（m×1）
多层就是反复 z = Wa + b; a = f(z)。

9. 前向传播、反向传播、损失/梯度/学习率
前向传播：数据从输入层一层层算到输出层，得到预测结果。
反向传播：从损失函数开始，用链式法则把误差逐层往回传，算出每个参数对损失的梯度。
损失函数：衡量模型预测与真实标签的差距。如均方误差 MSE、交叉熵 Cross-Entropy。
梯度：告诉参数该往哪个方向、调多少才能让损失下降。
学习率：控制每次参数更新的步长。太大震荡/发散，太小收敛慢。
10. 优化器
优化器：根据梯度更新参数的具体算法。
常见：
SGD（随机梯度下降）：最基础。
Momentum：给梯度加"惯性"，加速并减小震荡。
AdaGrad：自适应学习率，对稀疏特征好。
RMSProp：改进 AdaGrad 学习率单调下降问题。
Adam：结合 Momentum + RMSProp，目前最常用。
11. 过拟合、欠拟合、正则化
欠拟合：模型太简单，训练集都学不好。
过拟合：模型把训练集里的噪声也记住了，在训练集上很好、测试集上很差。
正则化：防止过拟合的手段。
L1/L2 正则：给损失函数加惩罚项，限制权重大小。
Dropout：训练时随机丢弃一部分神经元。
早停 Early Stopping：验证集 loss 不再下降就停止训练。
数据增强：人为扩充训练数据。


### 1. 为什么不同项目最好使用不同的 Python 虚拟环境？

每个项目依赖的第三方库版本经常不一样。比如项目 A 需要 PyTorch 2.0，项目 B 必须用 PyTorch 1.13；或者两个项目用的 numpy 版本互不兼容。

如果所有项目共用同一个 Python 环境，装新项目时很容易把旧项目的依赖覆盖掉，导致旧项目突然跑不通。虚拟环境相当于给每个项目单独开一间"隔离病房"，库各装各的，互不干扰。

另外，虚拟环境也方便项目迁移和复现。把 `requirements.txt` 或 `environment.yml` 发给别人，对方能一键还原出一模一样的环境。

---

### 2. CPU 和 GPU 各自擅长什么？训练神经网络为什么经常使用 GPU？

**CPU**：核心数少（通常几核到几十核），但单核性能强、时钟频率高、分支预测和复杂逻辑处理能力强。适合串行任务、操作系统调度、通用编程。

**GPU**：核心数极多（几千个），但每个核心相对简单。它天生适合把同一条指令同时作用在大量数据上，也就是"并行计算"。

神经网络训练本质上是一堆矩阵运算：前向传播算 $Wx+b$，反向传播算梯度，这些运算可以拆成很多互不依赖的小计算同时执行。GPU 的几千个核心正好能并行处理这些矩阵元素，所以训练速度通常比 CPU 快几十到几百倍。显存（VRAM）则用来存放模型参数和中间结果，batch size 越大越吃显存。

---

### 3. CUDA 是什么？它、显卡驱动与 PyTorch 之间大致是什么关系？

**CUDA**（Compute Unified Device Architecture）是 NVIDIA 提供的一套并行计算平台和编程接口，让开发者能用类似 C/C++ 的语法直接指挥 NVIDIA GPU 做通用计算。



